<h2>パスワード入力</h2>
<%= form_with(url: login_path, id: "password-form") do |f| %>
  <%= f.label :password, "パスワード" %>
  <%= f.password_field :password, autocomplete: "off" %>
  <%= f.submit '送信', class: 'btn btn-primary' %>
<% end %>
